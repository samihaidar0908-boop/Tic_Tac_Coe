// create a player object with name, mark, and score
// score = 0 is a default so it can counting score from 0
export const createPlayer = (name, mark , score = 0) => ({
    // return the name , mark, and score to Create player.
    name, mark , score
});